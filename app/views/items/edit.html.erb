<div class="main">
  <div class="inner">
    <div class="form__wrapper">
      <h2 class="page-heading">商品編集</h2>
      <%= form_with(model: @item, local: true) do |f| %>
      <%= render partial: 'form', locals: { form: f} %>
      <% end %>
    </div>
  </div>
</div>